from django.shortcuts import render

# Create your views here.
def login(request):
    return render(request, '')

def home(request):
    return render(request, '')

def book(request):
    return render(request, '')